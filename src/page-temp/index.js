/**
 * 临时文件夹，标签配置（字段->标签）使用
 * TODO: 完成后删掉
 */

import ReactDOM from 'react-dom'
import TagConfiguration from '../tag-configuration'

ReactDOM.render(<TagConfiguration />, document.getElementById('root'))
