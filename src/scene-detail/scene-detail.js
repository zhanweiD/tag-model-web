import {Component} from 'react'
import {observable, action} from 'mobx'
import {observer, inject, Provider} from 'mobx-react'


export default class SceneDetail extends Component {
  render() {
    return (
      <div className="scene-detail p16">
        scene-detail
      </div>
    )
  }
}
